'use strict';

// A mapping of route names to URLs
var urls = {
  index: '/'
};

// A map of route names to functions that return a URL
var resolve = {

};

module.exports = {
  resolve: resolve,
  urls: urls
};
